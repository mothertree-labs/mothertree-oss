apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: backend
    app.kubernetes.io/name: docs-backend
    app.kubernetes.io/part-of: mother-tree
  name: backend
  namespace: docs
spec:
  replicas: ${DOCS_BACKEND_MIN_REPLICAS}
  selector:
    matchLabels:
      io.kompose.service: backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.37.0 (HEAD)
      labels:
        io.kompose.service: backend
        app.kubernetes.io/component: backend
        app.kubernetes.io/instance: docs
        app.kubernetes.io/name: docs
    spec:
      containers:
        - name: backend
          image: lasuite/impress-backend:v5.6.1
          # Customizations no longer rewrite files in the image at start-up.
          # DJANGO_SETTINGS_MODULE=mt_settings (docs-config) loads /app/mt_settings.py,
          # a subclass of upstream's Production that adds MediaMiddleware, the
          # Linode storage backend and the mt_patches app (invitation-email
          # link); all three are mounted read-only from ConfigMaps below and
          # fail closed at import if upstream drifts. gunicorn's config file is
          # still shipped by the image (upstream's default CMD is uvicorn).
          command: ["/bin/sh", "-c"]
          args:
            - |
              exec gunicorn -c /usr/local/etc/gunicorn/impress.py --workers ${DOCS_GUNICORN_WORKERS} --timeout 30 impress.wsgi:application
          ports:
          - containerPort: 8000
            name: http
          envFrom:
          - configMapRef:
              name: docs-config
          - configMapRef:
              name: env-d-yprovider
          volumeMounts:
          - name: storage-backends
            mountPath: /app/storage_backends.py
            subPath: storage_backends.py
          - name: media-middleware
            mountPath: /app/impress/middleware.py
            subPath: middleware.py
          - name: mt-settings
            mountPath: /app/mt_settings.py
            subPath: mt_settings.py
            readOnly: true
          # /app/mt_patches does not exist in the image, so a directory mount
          # shadows nothing. /app is the image's WORKDIR and on sys.path.
          - name: mt-patches
            mountPath: /app/mt_patches
            readOnly: true
          env:
          # ConfigMap mounts are read-only; stop CPython trying to write
          # __pycache__ next to mt_settings.py / mt_patches/.
          - name: PYTHONDONTWRITEBYTECODE
            value: "1"
          - name: DJANGO_SECRET_KEY
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: DJANGO_SECRET_KEY
          - name: COLLABORATION_SERVER_SECRET
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: COLLABORATION_SERVER_SECRET
          - name: Y_PROVIDER_API_KEY
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: Y_PROVIDER_API_KEY
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: DATABASE_PASSWORD
          - name: AWS_S3_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: AWS_S3_ACCESS_KEY_ID
          - name: AWS_S3_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: AWS_S3_SECRET_ACCESS_KEY
          - name: OIDC_RP_CLIENT_SECRET
            valueFrom:
              secretKeyRef:
                name: docs-secrets
                key: OIDC_RP_CLIENT_SECRET
          # SMTP credentials — tenant Stalwart submission listener on port 588.
          # Secret is written by scripts/provision-smtp-service-accounts.
          # `optional: true` lets the pod start on a fresh tenant before the
          # secret is provisioned; callers can't send mail until then. Impress
          # settings use the DJANGO_ prefix (django-configurations default).
          - name: DJANGO_EMAIL_HOST
            valueFrom:
              secretKeyRef:
                name: smtp-credentials
                key: SMTP_RELAY_HOST
                optional: true
          - name: DJANGO_EMAIL_PORT
            valueFrom:
              secretKeyRef:
                name: smtp-credentials
                key: SMTP_RELAY_PORT
                optional: true
          - name: DJANGO_EMAIL_HOST_USER
            valueFrom:
              secretKeyRef:
                name: smtp-credentials
                key: SMTP_RELAY_USERNAME
                optional: true
          - name: DJANGO_EMAIL_HOST_PASSWORD
            valueFrom:
              secretKeyRef:
                name: smtp-credentials
                key: SMTP_RELAY_PASSWORD
                optional: true
          startupProbe:
            httpGet:
              path: /__lbheartbeat__
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 15
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /__heartbeat__
              port: 8000
            periodSeconds: 15
            timeoutSeconds: 15
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /__lbheartbeat__
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 15
            failureThreshold: 3
          # Memory tuned: actual usage ~375Mi steady state
          resources:
            requests:
              cpu: 100m  # Minimum 100m to prevent HPA triggering on idle fluctuations
              memory: 400Mi
            limits:
              memory: 1Gi
          securityContext:
            runAsUser: 0
      volumes:
      - name: storage-backends
        configMap:
          name: storage-backends
      - name: media-middleware
        configMap:
          name: storage-backends
          items:
            - key: middleware.py
              path: middleware.py
      - name: mt-settings
        configMap:
          name: docs-mt-python
          items:
            - key: mt_settings.py
              path: mt_settings.py
      - name: mt-patches
        configMap:
          name: docs-mt-python
          items:
            - key: __init__.py
              path: __init__.py
            - key: apps.py
              path: apps.py
      restartPolicy: Always
