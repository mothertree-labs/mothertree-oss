The `/create-pr` command lives in the **private config submodule** at:

```
config/platform/.claude/commands/create-pr.md
```

If the private config submodule is available, run that command instead. If it's not available, let the user know they need the private config submodule checked out to use the full PR creation workflow.
