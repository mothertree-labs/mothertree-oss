# Create Pull Request

Create a PR with automated compliance and security checks. The full instructions live in the private config submodule.

**Read `config/platform/.claude/commands/create-pr.md` and follow those instructions exactly.**

Pass through all user arguments (title, base branch, draft mode) to the instructions in that file.
