# Loki Log Query

Query application logs via Loki. The full skill instructions live in the private config submodule.

**Read `config/platform/.claude/commands/logs.md` and follow those instructions exactly.**

Pass through all user arguments (environment, component, time range, query) to the instructions in that file.
