<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - MotherTree</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            sage: { DEFAULT: '#7C9A6C', dark: '#5A7A4C' },
            cream: '#FAF8F5',
            'warm-gray': '#6B6B6B',
          }
        }
      }
    }
  </script>
</head>
<body class="bg-cream min-h-screen flex items-center justify-center p-8">
  <div class="bg-white rounded-xl shadow-lg p-8 max-w-md w-full">
    <div class="text-center mb-6">
      <h1 class="text-3xl text-sage mb-2">MotherTree</h1>
      <p class="text-warm-gray">Account Recovery</p>
    </div>

    <% if (success) { %>
      <!-- Success State -->
      <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
        <div class="flex items-center mb-2">
          <svg class="w-5 h-5 text-green-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
          <span class="text-green-800 font-medium">Recovery Email Sent</span>
        </div>
        <p class="text-green-700 text-sm">
          We've sent a passkey recovery link to <strong><%= success.recoveryEmailHint %></strong>.
        </p>
        <p class="text-green-700 text-sm mt-2">
          Check your recovery email inbox and click the link to re-register your passkey.
        </p>
      </div>

      <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
        <p class="text-amber-800 text-sm">
          <strong>Note:</strong> A notification has also been sent to your MotherTree email address for security purposes.
        </p>
      </div>

      <a href="/" class="block w-full bg-sage text-white px-6 py-3 rounded-lg hover:bg-sage-dark transition-colors text-center">
        Return to Home
      </a>
    <% } else { %>
      <!-- Form State -->
      <% if (error) { %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
          <div class="flex items-center">
            <svg class="w-5 h-5 text-red-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span class="text-red-800"><%= error %></span>
          </div>
        </div>
      <% } %>

      <p class="text-warm-gray text-sm mb-6">
        Lost access to your passkey? Enter both your MotherTree email and your recovery email to receive a link to re-register your passkey.
      </p>

      <form method="POST" action="/recover" class="space-y-4">
        <div>
          <label for="tenantEmail" class="block text-sm font-medium text-gray-700 mb-1">
            Your MotherTree Email
          </label>
          <input
            type="email"
            id="tenantEmail"
            name="tenantEmail"
            required
            placeholder="you@example.com"
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-sage focus:border-transparent outline-none"
          >
          <p class="text-xs text-warm-gray mt-1">The email address you use to sign in to MotherTree</p>
        </div>

        <div>
          <label for="recoveryEmail" class="block text-sm font-medium text-gray-700 mb-1">
            Your Recovery Email
          </label>
          <input
            type="email"
            id="recoveryEmail"
            name="recoveryEmail"
            required
            placeholder="yourname@gmail.com"
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-sage focus:border-transparent outline-none"
          >
          <p class="text-xs text-warm-gray mt-1">The external email address you provided when you joined</p>
        </div>

        <button
          type="submit"
          class="w-full bg-sage text-white px-6 py-3 rounded-lg hover:bg-sage-dark transition-colors font-medium"
        >
          Send Recovery Link
        </button>
      </form>

      <div class="mt-6 pt-4 border-t border-gray-200 text-center">
        <a href="/" class="text-sm text-warm-gray hover:text-sage">
          &larr; Back to Sign In
        </a>
      </div>
    <% } %>
  </div>
</body>
</html>
