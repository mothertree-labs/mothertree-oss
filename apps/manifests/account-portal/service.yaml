apiVersion: v1
kind: Service
metadata:
  name: account-portal
  labels:
    app: account-portal
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: account-portal
