# Dev: no SOCKS proxy — VPN server removed, probes go direct.
# The base values define http_2xx_ext and http_synapse_ext modules without proxy.
# If probes fail due to kube-proxy short-circuiting (PROXY protocol mismatch),
# consider routing through the Tailscale mesh or using internal ingress.
{}
