when:
  - event: pull_request
  - event: push
    branch: main

clone:
  - name: clone
    image: woodpeckerci/plugin-git
    settings:
      recursive: false

steps:
  - name: e2e-setup
    image: bash
    environment:
      CI: "true"
    commands:
      - ci/scripts/e2e-setup.sh
